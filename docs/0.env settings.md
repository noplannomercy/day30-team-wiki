🛠️ Step-by-Step 환경 구성
1️⃣ Next.js 프로젝트 생성
bashcd /home/claude/day28-travel-planner

# Next.js 프로젝트 생성
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*"

# 옵션 설명:
# --typescript: TypeScript 사용
# --tailwind: Tailwind CSS 포함
# --app: App Router 사용
# --no-src-dir: src 폴더 없이
# --import-alias "@/*": @ alias 설정

2️⃣ shadcn/ui 설치
bash# shadcn/ui 초기화
npx shadcn@latest init

# 질문 나오면:
# Style: Default
# Color: Slate
# CSS variables: Yes
# Install dependencies: Yes

3️⃣ 필요한 컴포넌트 설치
bash# 기본 UI 컴포넌트들
npx shadcn@latest add button
npx shadcn@latest add input
npx shadcn@latest add card
npx shadcn@latest add select
npx shadcn@latest add badge
npx shadcn@latest add tabs

4️⃣ 추가 패키지 설치
bash# Google Maps 관련
npm install @googlemaps/js-api-loader

# 유틸리티
npm install clsx tailwind-merge

# 아이콘 (선택)
npm install lucide-react

5️⃣ 환경 변수 설정
bash# .env.local 파일 생성
cat > .env.local << 'EOF'
GOOGLE_MAPS_API_KEY=AIzaSyBs2_7lqHKbkXSNOmkMWWCPBPcx-8cJDZQ
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=여기에_복사한_키_붙여넣기
EOF

# 주의:
# GOOGLE_MAPS_API_KEY: 서버 사이드용
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: 클라이언트 사이드용 (Maps JavaScript API)